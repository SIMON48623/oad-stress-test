# Protocol terminology note

Legacy frozen JSON field names ending in `_frames` refer to stride-four feature timesteps, not raw video frames. The manuscript consistently reports these quantities as feature timesteps. The names remain unchanged here to preserve the frozen audit trail.
